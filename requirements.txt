# Core Demucs dependencies
dora-search>=0.1.12
einops
julius>=0.2.3
lameenc>=1.2
openunmix
pyyaml
tqdm

# PyTorch - GPU users: install from CUDA index instead
#   uv pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu128
torch>=2.0.0
torchaudio>=2.0.0

# Enhanced pipeline (video/audio extraction + source separation output)
soundfile>=0.12.0
av>=12.0.0
